/**
 * defines the typography (text styles) used throughout the app.
 * uses Material3's Typography system, allowing you to later customize:
 *   - font families
 *   - sizes
 *   - weights (bold, medium, etc.)
 *   - line height
 * (currently this file just applies the default Material3 typography,
 * but it pretty much just gives you a centralized place to change global
 * text styling)
 */

package org.example.pagepalapp.ui.theme

import androidx.compose.material3.Typography

// typography definition: default (can be extended later.)
val AppTypography = Typography()
